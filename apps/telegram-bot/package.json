{
  "name": "@takeit/telegram-bot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Take It — Telegram bot conversation logic (framework-agnostic, Amharic-first). The Supabase edge function supabase/functions/telegram-webhook mirrors these modules; a sync test keeps the copies byte-identical.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@eslint/js": "^9.11.1",
    "@types/node": "^22.7.0",
    "eslint": "^9.11.1",
    "globals": "^15.10.0",
    "typescript": "~5.6.3",
    "typescript-eslint": "^8.8.1",
    "vitest": "^2.1.2"
  }
}
